# Tests Directory

This directory contains test suites for backend APIs, security boundaries, vector retrieval, and frontend components.

## Running Backend Tests

```bash
cd secure-legal-ai/backend
pytest ../tests/backend
```
